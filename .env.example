# LLM API 配置（用于自动填充标注字段）
# 不配置则仅生成骨架，需人工填充所有字段

# 必需：API 密钥
LLM_API_KEY=put-your-api-key-here

# 可选：API 端点（默认 OpenAI）
# LLM_BASE_URL=https://api.openai.com/v1

# 可选：模型名（默认 gpt-4o）
# LLM_MODEL=gpt-4o

# 可选：最大输出 token（默认 4096）
# LLM_MAX_TOKENS=4096

# 可选：温度（默认 0.3，越低越确定）
# LLM_TEMPERATURE=0.3
